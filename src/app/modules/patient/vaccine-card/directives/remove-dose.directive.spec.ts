import { RemoveDoseDirective } from './remove-dose.directive';

describe('RemoveDoseDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveDoseDirective();
    expect(directive).toBeTruthy();
  });
});
